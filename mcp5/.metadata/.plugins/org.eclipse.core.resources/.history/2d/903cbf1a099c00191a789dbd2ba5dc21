/*
 * 40.cpp
 *
 *  Created on: 2019/07/01
 *      Author: hhhhh
 */
#include <iostream>
#include <chrono>

using namespace std::chrono;

int durationDays(system_clock::time_point a, system_clock::time_point b){
	return duration_cast<hours>(a>b?a-b:b-a).count()/24;
}


int main(int argc, char **argv) {
	std::cout << durationDays(time_point<high_resolution_clock>(),system_clock::now()) << std::endl;
	std::cout << durationDays(system_clock::now(),time_point<high_resolution_clock>()) << std::endl;
	std::cout << durationDays(system_clock::now(),time_point(seconds(60*60*24)) << std::endl;

	return 0;
}
